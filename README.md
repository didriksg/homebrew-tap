# didriksg/tap

Personal Homebrew tap.

```sh
brew install --cask didriksg/tap/crisp
```

The app is ad-hoc signed; macOS will warn on first launch. Right-click
Crisp.app and choose Open, or install with `--no-quarantine`.
